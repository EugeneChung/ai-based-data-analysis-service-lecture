# Copy to .env and `export $(grep -v '^#' .env | xargs)` if you want a password.
# The gateway reads the same DELI_DB_* variables as deli-db-mcp, so both
# servers point at the same database.
DELI_DB_HOST=127.0.0.1
DELI_DB_PORT=3306
DELI_DB_USER=root
DELI_DB_PASSWORD=
DELI_DB_NAME=deli
